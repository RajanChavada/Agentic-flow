---
phase: 05-action-constraints
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - frontend/src/components/__tests__/TagInput.test.tsx
  - backend/tests/test_allowed_actions.py
autonomous: true
requirements: [ACTN-01, ACTN-02, ACTN-04, ACTN-05]

must_haves:
  truths:
    - "TagInput behavioral test file exists with tests covering add, remove, edit-last, duplicate rejection, and max tags"
    - "Backend allowed_actions test file exists with tests covering validation acceptance, validation rejection, and estimation impact"
  artifacts:
    - path: "frontend/src/components/__tests__/TagInput.test.tsx"
      provides: "Behavioral tests for TagInput component (RED until Plan 05-02 implements)"
      min_lines: 50
    - path: "backend/tests/test_allowed_actions.py"
      provides: "Validation and estimation tests for allowed_actions (RED until Plan 05-01 implements)"
      min_lines: 50
  key_links:
    - from: "frontend/src/components/__tests__/TagInput.test.tsx"
      to: "frontend/src/components/ui/TagInput.tsx"
      via: "imports TagInput component (will resolve after Plan 05-02)"
      pattern: "import.*TagInput"
    - from: "backend/tests/test_allowed_actions.py"
      to: "backend/models.py"
      via: "imports NodeConfig for validation tests (will pass after Plan 05-01)"
      pattern: "from models import NodeConfig"
---

<objective>
Create test scaffolds for both the TagInput frontend component and backend allowed_actions validation/estimation before implementation begins.

Purpose: Establishes RED phase of TDD -- tests define expected behavior before code exists. Plan 05-01 turns backend tests GREEN, Plan 05-02 turns frontend tests GREEN.
Output: Two test files with failing tests that define the behavioral contract.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-action-constraints/05-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create TagInput behavioral test file</name>
  <files>frontend/src/components/__tests__/TagInput.test.tsx</files>
  <behavior>
    - Test 1: Renders with empty value and shows input placeholder
    - Test 2: Displays existing tags as visible text chips
    - Test 3: Calls onChange with new tag appended on Enter key press
    - Test 4: Rejects empty/whitespace-only input on Enter (no onChange call)
    - Test 5: Rejects duplicate tags case-insensitively (no onChange call)
    - Test 6: Calls onChange with tag removed when X button clicked
    - Test 7: Calls onChange with last tag removed on Backspace when input is empty
    - Test 8: Shows helper text when value is empty and input is empty
    - Test 9: Respects maxTags limit (no onChange when at max)
  </behavior>
  <action>
Create `frontend/src/components/__tests__/TagInput.test.tsx`.

Import from `@testing-library/react` (render, screen, fireEvent) and `vitest` (describe, test, expect, vi).
Import TagInput from `../ui/TagInput` (will not resolve until 05-02 creates the component -- this is expected RED state).

Write 9 test cases in a `describe("TagInput", ...)` block:

1. `"renders input with placeholder"`: render `<TagInput value={[]} onChange={vi.fn()} placeholder="Type action..." />`, assert `screen.getByPlaceholderText("Type action...")` is in the document.

2. `"displays existing tags as chips"`: render with `value={["approve", "reject"]}`, assert both text values are visible via `screen.getByText("approve")` and `screen.getByText("reject")`.

3. `"adds tag on Enter"`: render with `value={[]}` and `onChange={vi.fn()}`, get input via placeholder, fire `change` event with value "approve", fire `keyDown` with `{ key: "Enter" }`, assert `onChange` called with `["approve"]`.

4. `"rejects empty input on Enter"`: render with `value={[]}` and `onChange={vi.fn()}`, fire `keyDown` with `{ key: "Enter" }` on empty input, assert `onChange` NOT called.

5. `"rejects duplicate case-insensitively"`: render with `value={["approve"]}` and `onChange={vi.fn()}`, type "Approve" into input, press Enter, assert `onChange` NOT called.

6. `"removes tag on X click"`: render with `value={["approve", "reject"]}` and `onChange={vi.fn()}`, find X buttons (use `screen.getAllByRole("button")`), click first one, assert `onChange` called with `["reject"]`.

7. `"backspace on empty removes last tag"`: render with `value={["approve", "reject"]}` and `onChange={vi.fn()}`, ensure input is empty, fire `keyDown` with `{ key: "Backspace" }`, assert `onChange` called with `["approve"]`.

8. `"shows helper text when empty"`: render with `value={[]}`, assert `screen.getByText(/Define allowed actions/)` is in the document.

9. `"respects maxTags"`: render with `value={["a", "b"]}`, `maxTags={2}`, and `onChange={vi.fn()}`, type "c", press Enter, assert `onChange` NOT called.

These tests will all FAIL (RED) at this stage because TagInput.tsx does not exist yet. This is expected -- Plan 05-02 brings them to GREEN.
  </action>
  <verify>
    <automated>ls /Users/jimmychavada/Documents/Agentic-flow/frontend/src/components/__tests__/TagInput.test.tsx && echo "TagInput test file exists"</automated>
  </verify>
  <done>TagInput.test.tsx exists with 9 behavioral tests covering add, remove, edit-last, duplicate rejection, empty state, and maxTags. Tests are in RED state (expected -- component does not exist yet).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create backend allowed_actions test file</name>
  <files>backend/tests/test_allowed_actions.py</files>
  <behavior>
    - Test 1: NodeConfig accepts valid allowed_actions list (e.g., ["approve", "reject"])
    - Test 2: NodeConfig accepts allowed_actions=None (optional field)
    - Test 3: NodeConfig rejects allowed_actions with empty string item -> ValidationError
    - Test 4: NodeConfig rejects allowed_actions with >20 items -> ValidationError
    - Test 5: NodeConfig rejects allowed_actions with item >50 chars -> ValidationError
    - Test 6: NodeConfig strips whitespace from action items
    - Test 7: estimate_agent_node with classification task + 3 allowed_actions produces fewer output_tokens than without
    - Test 8: estimate_agent_node with routing task + allowed_actions produces fewer output_tokens than without
    - Test 9: estimate_agent_node with summarization task + allowed_actions does NOT change output_tokens
    - Test 10: estimate_agent_node with classification + allowed_actions adds action label tokens to input_tokens
  </behavior>
  <action>
Create `backend/tests/test_allowed_actions.py`.

Import `pytest`, `from pydantic import ValidationError`, `from models import NodeConfig`, `from estimator import estimate_agent_node`.

**Validation tests (6 tests) in class TestAllowedActionsValidation:**

- `test_allowed_actions_accepts_valid_list`: Create `NodeConfig(id="t", type="agentNode", allowed_actions=["approve", "reject"])`, assert `nc.allowed_actions == ["approve", "reject"]`.
- `test_allowed_actions_accepts_none`: Create `NodeConfig(id="t", type="agentNode")` with no `allowed_actions`, assert `nc.allowed_actions is None`.
- `test_allowed_actions_rejects_empty_string`: `pytest.raises(ValidationError)` for `NodeConfig(id="t", type="agentNode", allowed_actions=[""])`.
- `test_allowed_actions_rejects_over_20_items`: `pytest.raises(ValidationError)` for `NodeConfig(id="t", type="agentNode", allowed_actions=["x"] * 21)`.
- `test_allowed_actions_rejects_over_50_chars`: `pytest.raises(ValidationError)` for `NodeConfig(id="t", type="agentNode", allowed_actions=["x" * 51])`.
- `test_allowed_actions_strips_whitespace`: Create with `["  approve  "]`, assert result is `["approve"]`.

All NodeConfig instances use `id="t", type="agentNode"` as minimum required fields.

**Estimation tests (4 tests) in class TestAllowedActionsEstimation:**

Create a helper `_make_node(task_type, actions=None)` that returns:
```python
NodeConfig(
    id="t", type="agentNode",
    model_provider="OpenAI", model_name="GPT-4o",
    task_type=task_type, expected_output_size="short",
    allowed_actions=actions,
)
```

- `test_classification_actions_reduce_output_tokens`: Compare `estimate_agent_node(_make_node("classification"), [])` vs `estimate_agent_node(_make_node("classification", ["a", "b", "c"]), [])`. Assert `with_actions.output_tokens < without.output_tokens`.
- `test_routing_actions_reduce_output_tokens`: Same pattern for `"routing"` task type. Assert fewer output tokens with actions.
- `test_summarization_unaffected_by_actions`: Same pattern for `"summarization"`. Assert `with_actions.output_tokens == without.output_tokens`.
- `test_classification_actions_increase_input_tokens`: Compare input_tokens for classification with and without `["approve", "reject", "escalate"]`. Assert `with_actions.input_tokens > without.input_tokens`.

These tests will FAIL (RED) at this stage because `allowed_actions` field and estimation logic don't exist on NodeConfig/estimator yet. Plan 05-01 brings them to GREEN.
  </action>
  <verify>
    <automated>cd /Users/jimmychavada/Documents/Agentic-flow/backend && python -c "import ast; ast.parse(open('tests/test_allowed_actions.py').read()); print('Syntax OK')"</automated>
  </verify>
  <done>test_allowed_actions.py exists with 10 tests (6 validation + 4 estimation). Tests are in RED state (expected -- allowed_actions field and estimation logic don't exist yet).</done>
</task>

</tasks>

<verification>
1. `ls frontend/src/components/__tests__/TagInput.test.tsx` -- file exists
2. `ls backend/tests/test_allowed_actions.py` -- file exists
3. Backend test file is valid Python syntax (ast.parse succeeds)
</verification>

<success_criteria>
- TagInput.test.tsx exists with 9 behavioral tests
- test_allowed_actions.py exists with 10 tests (6 validation + 4 estimation)
- Both files are syntactically valid
- Tests are expected to FAIL (RED state) -- Plans 05-01 and 05-02 will turn them GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/05-action-constraints/05-00-SUMMARY.md`
</output>

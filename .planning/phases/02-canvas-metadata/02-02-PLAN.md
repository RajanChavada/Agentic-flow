---
phase: 02-canvas-metadata
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - frontend/src/components/CanvasMetadataOverlay.tsx
  - frontend/src/components/__tests__/CanvasMetadataOverlay.test.tsx
  - frontend/src/components/Canvas.tsx
autonomous: false
requirements: [META-01, META-02, META-03, META-04, META-05, META-06, ESTM-04]

must_haves:
  truths:
    - "User sees overlay in top-right corner showing node count, depth, loop count"
    - "User sees abbreviated tool risk surface (R:W:X:N) color-coded by type"
    - "User sees risk score label (Low green / Medium amber / High red)"
    - "User sees reachability indicator (Reachable checkmark / Not reachable X / dash placeholder when no ideal state)"
    - "Overlay updates reactively when nodes or edges change with no API calls"
    - "No visible UI jank when dragging nodes rapidly on canvas"
  artifacts:
    - path: "frontend/src/components/CanvasMetadataOverlay.tsx"
      provides: "Frosted glass overlay component with all metadata display"
      min_lines: 40
    - path: "frontend/src/components/__tests__/CanvasMetadataOverlay.test.tsx"
      provides: "Unit tests verifying memoized graph analysis and metric rendering"
      min_lines: 30
    - path: "frontend/src/components/Canvas.tsx"
      provides: "Canvas with CanvasMetadataOverlay integrated as ReactFlow child"
      contains: "CanvasMetadataOverlay"
  key_links:
    - from: "frontend/src/components/CanvasMetadataOverlay.tsx"
      to: "frontend/src/lib/graphAnalysis.ts"
      via: "import { analyzeGraph } from @/lib/graphAnalysis"
      pattern: "import.*analyzeGraph.*from.*graphAnalysis"
    - from: "frontend/src/components/CanvasMetadataOverlay.tsx"
      to: "frontend/src/store/useWorkflowStore.ts"
      via: "Fine-grained Zustand selectors for nodes and edges"
      pattern: "useWorkflowStore.*s\\.nodes"
    - from: "frontend/src/components/Canvas.tsx"
      to: "frontend/src/components/CanvasMetadataOverlay.tsx"
      via: "JSX child inside ReactFlow element"
      pattern: "<CanvasMetadataOverlay"
---

<objective>
Create the CanvasMetadataOverlay component and wire it into Canvas.tsx. The overlay displays real-time graph metrics in a frosted glass HUD at the top-right corner of the canvas, updating reactively as users add/remove/connect nodes.

Purpose: This is the visual layer that makes graph analysis visible to users. It turns the `analyzeGraph` computations from Plan 01 into a human-readable, always-visible HUD with color-coded risk indicators and reachability status.

Output: Working overlay component integrated into the canvas showing all 7 metadata metrics per CONTEXT.md specifications.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-canvas-metadata/02-CONTEXT.md
@.planning/phases/02-canvas-metadata/02-RESEARCH.md
@.planning/phases/02-canvas-metadata/02-01-SUMMARY.md
@frontend/src/components/Canvas.tsx
@frontend/src/store/useWorkflowStore.ts

<interfaces>
<!-- From Plan 01 output: graphAnalysis.ts exports -->
From frontend/src/lib/graphAnalysis.ts (created in Plan 02-01):
```typescript
export interface GraphMetrics {
  nodeCount: number;
  workflowNodeCount: number;
  maxDepth: number;
  loopCount: number;
  toolRiskSurface: {
    read: number;
    write: number;
    exec: number;
    network: number;
    other: number;
  };
  riskScore: number;
  riskLevel: 'Low' | 'Medium' | 'High';
  idealStateReachable: boolean | null;
}

export function analyzeGraph(nodes: Node[], edges: Edge[]): GraphMetrics;
```

From frontend/src/store/useWorkflowStore.ts (existing selectors):
```typescript
export const useWorkflowNodes = () => useWorkflowStore((s) => s.nodes);
export const useWorkflowEdges = () => useWorkflowStore((s) => s.edges);
export const useUIState = () => useWorkflowStore((s) => s.ui);
// ui contains: { theme: "light" | "dark", ... }
```

From Canvas.tsx (integration point -- overlay goes inside ReactFlow as sibling to MiniMap):
```tsx
<ReactFlow ...>
  <Background ... />
  <Controls ... />
  <MiniMap ... />
  {/* CanvasMetadataOverlay goes here */}
</ReactFlow>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CanvasMetadataOverlay component with unit tests and wire into Canvas</name>
  <files>frontend/src/components/CanvasMetadataOverlay.tsx, frontend/src/components/__tests__/CanvasMetadataOverlay.test.tsx, frontend/src/components/Canvas.tsx</files>
  <action>
    **Create `frontend/src/components/CanvasMetadataOverlay.tsx`:**

    Add `"use client"` and `"use no memo"` directives at top (required for all React Flow component files per CLAUDE.md).

    Import dependencies:
    - `useMemo` from "react"
    - `analyzeGraph` and `GraphMetrics` from "@/lib/graphAnalysis"
    - `useWorkflowStore` from "@/store/useWorkflowStore" (use inline selector, NOT `useWorkflowNodes` -- selector must grab `s.nodes` and `s.edges` directly for fine-grained reactivity)
    - Lucide icons: `CheckCircle2`, `XCircle`, `Minus` from "lucide-react"

    Component implementation:
    1. Subscribe to `nodes` and `edges` via two separate `useWorkflowStore(s => s.nodes)` and `useWorkflowStore(s => s.edges)` calls (fine-grained selectors per Zustand performance pattern -- avoids re-render on unrelated store changes like theme, estimation, modal state)
    2. Wrap `analyzeGraph(nodes, edges)` in `useMemo` with `[nodes, edges]` dependency array -- this ensures recomputation only when nodes/edges change, not on every render
    3. Render the overlay `div` with these Tailwind classes for frosted glass per CONTEXT.md locked decision:
       - Position: `absolute top-4 right-4 z-50` (above MiniMap which uses z-10)
       - Frosted glass: `bg-white/80 dark:bg-gray-900/80 backdrop-blur-md`
       - Border: `rounded-lg border border-gray-200 dark:border-gray-700`
       - Shadow: `shadow-lg`
       - Typography: `text-sm font-medium text-gray-900 dark:text-gray-100`
       - Interaction: `pointer-events-none select-none` (overlay is read-only HUD, should not interfere with canvas interactions)
       - Padding: `px-3 py-2`
    4. Render metrics in a single-row `flex items-center gap-2 whitespace-nowrap` layout:
       - Node count: `{metrics.workflowNodeCount} nodes` (use workflowNodeCount, not total nodeCount)
       - Separator: `<span className="text-gray-400">|</span>` between each metric
       - Depth: `depth {metrics.maxDepth}`
       - Loop count: `{metrics.loopCount} loops`
       - Risk surface abbreviated per CONTEXT.md: `R:{read}` in blue-600, `W:{write}` in amber-600, `X:{exec}` in red-600, `N:{network}` in purple-600 (each with dark mode variants: blue-400, amber-400, red-400, purple-400)
       - Risk level: `{metrics.riskLevel}` with conditional color classes:
         - Low: `text-green-600 dark:text-green-400`
         - Medium: `text-amber-600 dark:text-amber-400`
         - High: `text-red-600 dark:text-red-400`
       - Reachability indicator:
         - `null` (no ideal state node): Lucide `Minus` icon (w-4 h-4) + "--" text in `text-gray-400`
         - `true` (reachable): Lucide `CheckCircle2` icon (w-4 h-4) + "Reachable" in `text-green-600 dark:text-green-400`
         - `false` (not reachable): Lucide `XCircle` icon (w-4 h-4) + "Not reachable" in `text-red-600 dark:text-red-400`
         - Each reachability option wrapped in `flex items-center gap-1`

    Export as named export: `export function CanvasMetadataOverlay()`

    **Create `frontend/src/components/__tests__/CanvasMetadataOverlay.test.tsx`:**

    Unit test file verifying component behavior. Mock dependencies:
    - Mock `@/lib/graphAnalysis` module: `vi.mock("@/lib/graphAnalysis", () => ({ analyzeGraph: vi.fn() }))`
    - Mock `@/store/useWorkflowStore`: return controlled `nodes` and `edges` arrays
    - Mock `lucide-react` icons as simple span elements

    Test cases:
    1. **"memoizes graph analysis"**: Render component twice with same nodes/edges references. Assert `analyzeGraph` is called once (useMemo caches). Then update nodes reference. Assert `analyzeGraph` is called again. This verifies the useMemo dependency array is correct.
    2. **"renders metric values from analyzeGraph"**: Mock `analyzeGraph` to return a known `GraphMetrics` object (e.g., `{ workflowNodeCount: 5, maxDepth: 3, loopCount: 1, ... riskLevel: 'Medium', idealStateReachable: true }`). Assert rendered output contains "5 nodes", "depth 3", "1 loops", "Medium", "Reachable".
    3. **"renders dash placeholder when idealStateReachable is null"**: Mock `analyzeGraph` returning `idealStateReachable: null`. Assert rendered output contains "--".

    **Modify `frontend/src/components/Canvas.tsx`:**

    1. Add import at top (after existing component imports):
       ```typescript
       import { CanvasMetadataOverlay } from "@/components/CanvasMetadataOverlay";
       ```
    2. Add `<CanvasMetadataOverlay />` as the last child inside `<ReactFlow>`, after `<MiniMap>`:
       ```tsx
       <MiniMap ... />
       <CanvasMetadataOverlay />
       ```

    **What to avoid and WHY:**
    - Do NOT use `useWorkflowNodes()` or `useWorkflowEdges()` hooks -- they are re-exported selector hooks but using `useWorkflowStore(s => s.nodes)` directly is equivalent and makes the subscription pattern explicit for this performance-critical component
    - Do NOT add debouncing or requestAnimationFrame -- React 18+ automatic batching already coalesces updates within the same event loop tick; adding debounce would introduce 100-300ms visible lag
    - Do NOT subscribe to `useWorkflowStore()` without a selector (whole store subscription would cause re-renders on every modal open, theme toggle, estimation update, etc.)
    - Do NOT use `useEffect` for derived state -- `useMemo` is synchronous and avoids the 1-frame lag that useEffect would introduce
    - Do NOT use emojis -- use Lucide icons per CLAUDE.md hard rules
    - Do NOT use CSS modules -- Tailwind utility classes only per project conventions
    - Do NOT use `!bg-white` syntax -- use `bg-white!` (Tailwind v4 syntax per CLAUDE.md)
  </action>
  <verify>
    <automated>cd /Users/jimmychavada/Documents/Agentic-flow/frontend && npx vitest run src/components/__tests__/CanvasMetadataOverlay.test.tsx</automated>
    Also run TypeScript compilation:
    ```bash
    cd /Users/jimmychavada/Documents/Agentic-flow/frontend && npx tsc --noEmit
    ```
    Both must pass. The Vitest run verifies behavioral correctness (memoization, metric rendering, reachability placeholder). The tsc check verifies type safety across the component and Canvas integration.
  </verify>
  <done>
    - CanvasMetadataOverlay.tsx exists with frosted glass overlay rendering all 7 metrics
    - CanvasMetadataOverlay.test.tsx exists with passing tests for memoization, metric rendering, and reachability placeholder
    - Canvas.tsx imports and renders CanvasMetadataOverlay inside ReactFlow
    - TypeScript compiles with no errors
    - Overlay uses fine-grained Zustand selectors (nodes, edges only)
    - Overlay uses useMemo for graph analysis (no useEffect)
    - All colors match CONTEXT.md specification (risk surface: blue/amber/red/purple; risk level: green/amber/red)
    - Reachability shows dash placeholder when no ideal state node exists
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify overlay visual appearance and real-time behavior</name>
  <files>frontend/src/components/CanvasMetadataOverlay.tsx</files>
  <action>
    Human verifies the canvas metadata overlay visually and functionally.

    What was built: Canvas metadata overlay -- a frosted glass HUD in the top-right corner showing real-time graph metrics. Displays: node count, max depth, loop count, tool risk surface (R:W:X:N color-coded), aggregate risk score (Low/Medium/High), and Ideal State reachability indicator.

    Steps to verify:
    1. Start the frontend dev server: `cd frontend && npm run dev`
    2. Open browser to http://localhost:3000 and navigate to the editor (guest mode or any canvas)
    3. Verify overlay appears: Look for frosted glass panel in top-right corner of canvas. Should show "0 nodes | depth 0 | 0 loops | R:0 W:0 X:0 N:0 | Low | -- --"
    4. Verify real-time updates: Drag a Start node onto canvas from sidebar. Overlay should update to "1 nodes | depth 0 | 0 loops | ..."
    5. Verify node counting: Add an Agent node and connect Start->Agent. Check: "2 nodes | depth 1". Add a Tool node -- check: "3 nodes". Add a blankBox/textNode -- check: workflow node count should NOT increase (annotations excluded)
    6. Verify tool risk surface: Configure a tool node with a category (e.g., code_execution). Risk surface should update -- "X:1" should appear in red
    7. Verify risk score: With code_execution tool, risk should show "Low" (score 2). Add more risky tools until Medium/High threshold
    8. Verify reachability: With no Ideal State Node, should show dash "--" placeholder. (Ideal State Node is Phase 3, so this should default to "--")
    9. Verify drag performance: Drag nodes rapidly around the canvas for 5-10 seconds. Overlay should update smoothly with no visible jank or lag
    10. Verify dark mode: Toggle dark mode if available. Overlay should have dark background with light text. Frosted glass effect should still be visible
    11. Verify overlay does not block interaction: Try clicking/dragging nodes beneath the overlay area -- pointer-events-none should let interactions pass through
  </action>
  <verify>Manual visual verification by user</verify>
  <done>User confirms overlay appearance, real-time reactivity, drag performance, dark mode, and non-blocking interaction are all correct</done>
</task>

</tasks>

<verification>
1. Unit tests: `cd frontend && npx vitest run src/components/__tests__/CanvasMetadataOverlay.test.tsx` passes with 0 failures
2. TypeScript compilation: `cd frontend && npx tsc --noEmit` passes with 0 errors
3. Graph analysis tests: `cd frontend && npx vitest run src/lib/__tests__/graphAnalysis.test.ts` passes (from Plan 01)
4. Visual verification: Overlay visible in top-right, updates reactively, no jank during drag
5. Dark mode: Overlay readable in both light and dark themes
</verification>

<success_criteria>
- Overlay renders in top-right corner with frosted glass styling
- Shows all 7 metrics: node count, depth, loops, R:W:X:N surface, risk level, reachability
- Updates in real-time when nodes/edges change (no API calls)
- No visible UI jank when dragging nodes rapidly
- Dark mode compatible (light and dark themes work)
- Reachability shows "--" when no Ideal State Node exists (Phase 3 unlocks full reachability)
- TypeScript compiles with 0 errors
- Unit tests pass verifying memoization and metric rendering
- Overlay does not block canvas interaction (pointer-events-none)
</success_criteria>

<output>
After completion, create `.planning/phases/02-canvas-metadata/02-02-SUMMARY.md`
</output>
